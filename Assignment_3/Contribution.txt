MNS2025031

Added pages/chat.js and pages/index.js to create a basic real-time chat UI using Next.js and Socket.io.

index.js: simple login page that stores the username and redirects to the chat interface.

chat.js: main chat interface with message history, real-time updates, and online user list.


MNS2025030: added pacakage.json and package-lock.json
1. package.json – Defines your Node.js project’s metadata, scripts, and dependencies (acts as the main configuration file for npm).

2. package-lock.json – Records the exact versions of installed dependencies to ensure consistent installs across environments.



## MNS2025029

(1) models/Message.js → Defines the MongoDB schema and model for storing chat messages (sender, receiver, text, timestamp) using Mongoose.

(2)server.js → Acts as the main backend server, combining Express, Socket.io, and Next.js to handle real-time messaging, user connections, and message storage.

(3)pages/api folder handle user authentication
   register creates a new user by storing their username and password in MongoDB
   register creates a new user by storing their username and password in MongoDB



